{
  "name": "holdings-parser-local",
  "private": true,
  "scripts": {
    "test": "tsc -p tsconfig.test.json && node --test holdings-csv-parser.test.cjs review-export-surface.test.cjs emoney-browser-helper.test.cjs",
    "typecheck": "tsc --noEmit --lib es2019,dom holdings-schema.ts holdings-csv-parser.ts review-export-surface.ts emoney-browser-helper.ts main.ts",
    "build:demo": "node scripts/build-demo.mjs",
    "start:demo": "npm run build:demo && node scripts/serve-demo.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
