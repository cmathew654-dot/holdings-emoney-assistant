{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local Holdings eMoney Assistant window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
